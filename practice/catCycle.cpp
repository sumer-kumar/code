#include <bits/stdc++.h>
using namespace std;
#define ll long long




int main() {
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);


	ll test;
	cin >> test;
	ll size;
	ll k;
	ll a, b;
	while (test--) {
		cin >> size >> k;

		a = size;
		b = 1;
		while (k - 1) {
			ll temp = (size + 1) % 2;



		}

		return 0;
	}
